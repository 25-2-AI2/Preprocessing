# ============================================================
# 라벨링 도구 설정 파일
# ============================================================

# ------------------------------------------------------------
# API 설정
# ------------------------------------------------------------
# 사용할 OpenAI 모델
# 옵션: gpt-4o-mini (속도 우선), gpt-4o (품질 우선)
MODEL = "gpt-4o-mini"

# API 요청 타임아웃 (초)
API_TIMEOUT = 120

# ------------------------------------------------------------
# 성능 설정
# ------------------------------------------------------------
# 한 번에 API로 보낼 리뷰 개수 (10-100 권장)
BATCH_SIZE = 50

# 동시 실행 API 호출 수 (5-15 권장)
MAX_CONCURRENCY = 10

# 체크포인트 저장 간격 (배치 단위)
CHECKPOINT_INTERVAL = 5

# API 재시도 횟수
MAX_RETRIES = 5

# 재시도 최소 대기 시간 (초)
RETRY_MIN_WAIT = 2

# 재시도 최대 대기 시간 (초)
RETRY_MAX_WAIT = 60

# ------------------------------------------------------------
# 데이터 컬럼 설정
# ------------------------------------------------------------
# 리뷰 텍스트가 있는 컬럼명
TEXT_COLUMN = "cleaned_text"

# 리뷰 고유 ID 컬럼명
ID_COLUMN = "review_id"

# ------------------------------------------------------------
# 디렉토리 설정
# ------------------------------------------------------------
# 기본 입력 디렉토리
DEFAULT_INPUT_DIR = "parquet_data"

# 기본 출력 디렉토리 (Parquet)
DEFAULT_OUTPUT_DIR = "parquet_data"

# 체크포인트/중간파일/CSV 저장 디렉토리
CHECKPOINT_DIR = "label_data"
